# ─── Runtime Data (never commit) ────────────────────────────────────────────
data/
# Exception: web/data/ contains sample data files for the standalone HTML dashboards
!web/data/
logs/
sessions/
state.db*
memories/MEMORY.md.lock
audio_cache/
image_cache/
whatsapp/
pairing/
auth.lock
.hermes_history

# ─── SQLite runtime ────────────────────────────────────────────────────────
*.db
*.db-shm
*.db-wal

# ─── Generated / cached ──────────────────────────────────────────────────────
__pycache__/
*.pyc
scripts/__pycache__/
scripts/signals.json
scripts/loss_cooldowns.json
scripts/pending-delayed-entries.json
scripts/trailing_stops.json
scripts/trade_patterns.json
migration/
*.zip
*.tar.gz

# ─── Backup / temp / lock files ─────────────────────────────────────────────
*.bak
*.bak-*
*.save
*.save.*
*.tmp
*.lock
*.corrupt
*.pyc

# ─── Environment secrets ──────────────────────────────────────────────────────
.env

# ─── Large / downloadable artifacts ─────────────────────────────────────────
bin/
checkpoints/
models_dev_cache.json

# ─── Seed data (download from /git endpoint instead) ─────────────────────
seed/
seed/*.sql
.secrets.local
skills/openclaw-imports/
channel_directory.json
gateway.pid
gateway_state.json
cron/output/
cron/.tick.lock
cron/jobs.json

# ─── Sensitive / Personal (never commit) ────────────────────────────────────
memories/MEMORY.md
memories/MEMORY.md.lock
memories/USER.md
memories/memory-index.md
memories/hyperliquid.md
interrupt_debug.log
config.yaml

# ─── Secrets ─────────────────────────────────────────────────────────────────
.secrets.local
.skills_prompt_snapshot.json
scripts/signals_hermes_runtime.db
pastes/
scripts/runtime

# ─── W&B ──────────────────────────────────────────────────────────────────────
wandb/
scripts/wandb/offline-run-*/
scripts/wandb/debug*.log
scripts/wandb/latest-run
wandb-local/

# ─── Runtime dirs (never commit) ─────────────────────────────────────────────
memories/
lsp/
spawn-trees/
state-snapshots/
vids/

# ─── Test scratch files ──────────────────────────────────────────────────────
scripts/test_*.py
scripts/tests/

# ─── API caches ──────────────────────────────────────────────────────────────
provider_models_cache.json
ollama_cloud_models_cache.json

# ─── Cron runtime ────────────────────────────────────────────────────────────
cron/.jobs.lock
cron/.jobs_*.tmp
cron/ticker_heartbeat
cron/ticker_last_success

# ─── Debug scratch ───────────────────────────────────────────────────────────
pipeline_bug_report_*.txt
pipeline_health_report_*.txt
auth.json.corrupt
.npm_lock_hash_*

# ─── Skills internal state (bundled skills, not our code) ────────────────────
skills/.archive/
skills/.curator_backups/
skills/.curator_state
skills/.hub/
skills/.usage.json
skills/.usage.json.lock

# ─── Duplicate plans dir ─────────────────────────────────────────────────────
.hermes/plans/

# ─── Runtime caches ──────────────────────────────────────────────────────────
cache/delegation/
data/combo_weights.json

# ─── Cleanup: files kept locally, not in repo ──────────────────────────────
autopilot.md
processes.json
.update_check
hermes-self-close-watcher.service
books/
archive/
wandb-local/
graphify-out/
pastes/
reviews/
summaries/
reports/
plans/
