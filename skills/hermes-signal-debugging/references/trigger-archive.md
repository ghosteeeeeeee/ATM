# hermes-signal-debugging — Trigger Archive

Archived trigger entries too large or stale to keep in the main SKILL.md. These remain searchable and are still consulted by the skill.

## Archived Triggers (from SKILL.md)

<!-- Keep this file in sync when editing triggers in the main SKILL.md -->

Old/bulky triggers moved here to stay under the 100K SKILL.md limit:

- "stale signals in hot-set after regime change"
- "phase_accel not appearing in hot-set"
- "LAYER-type move not detected"
- "zscore-momentum tuner sweeps 0 tokens"
- "signals not combining"
- "guardian reopens after TP close"
- "same signal reopens after TP close"
- "guardian reopens after profit"
- "closing marker blocks token permanently"
- "guardian orphan trade_id mismatch"
- "phantom trade"
- "rate limit 429"
- "wave_phase=neutral for all tokens"
- "signal-logic-review claims X is missing but code already has it — external review is outdated"
- "accel-300 zero signals after stale fix, chop filter still too tight"
- "accel-300 SHORT never fires despite strong downside momentum"
- "accel-300 detects oldest qualifying bar instead of most recent — stale signals firing"
- "rs.py recency score formula inverted — ancient touches weighted more than recent"
- "rs.py bounce confirmation is a soft gate — should be hard gate"
- "rs.py RS_TOUCH_HARD_CAP bypassed when constant is None or 0"
- "signal-logic-review full audit — fix status report needed"
