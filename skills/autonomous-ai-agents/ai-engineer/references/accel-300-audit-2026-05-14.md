# accel_300.py Audit — 2026-05-14

**File:** `/root/.hermes/scripts/signals/accel_300.py` (521 lines)
**Syntax:** Clean | **Constants:** Verified

## Bugs Found

### P0 — Final-verify checks wrong bar (lines 319-336)
```python
current_bar_idx = len(closes) - 2   # ← ALWAYS newest bar in dataset
if current_bar_idx != i:
    if direction == 'LONG' and not (closes[current_bar_idx] > ema300[current_bar_idx]):
        continue   # ← blocks signal at i because bar n-2 reversed after detection
```
Signal detected at i=695 (gap growing 0.30→0.33), but bar 698 (n-2) shows gap=-0.02 → blocked.

**Fix:** Verify against `i` (the bar where signal was detected), not `len(closes)-2`.
If staleness check is needed, add explicit `bars_since_cross > N` before return.

---

### P1 — gap_bonus threshold (0.05%) misaligned from MIN_GAP_GROWTH (0.03%) — line 426
```python
gap_bonus = max(0, sig['gap_growth'] - 0.05) * 200  # bonus threshold = 0.05
# but ACCEL_300_MIN_GAP_GROWTH = 0.03  ← signal fires at 0.03, bonus at 0.05
```
Signals that clear 0.03% threshold but have growth < 0.05% get zero gap_bonus.

**Fix:** Use `ACCEL_300_MIN_GAP_GROWTH` instead of hardcoded `0.05`, or define a separate `GAP_BONUS_THRESHOLD`.

---

### P1 — Stale comment (line 69)
```python
# MIN_GAP_PCT sourced from hermes_constants: MIN_GAP_PCT_LONG / MIN_GAP_PCT_SHORT
```
Actual usage (line 222): `MIN_GAP_PCT_LONG` for LONG, `MIN_GAP_PCT_SHORT` for SHORT.
Comment implies single `MIN_GAP_PCT` but it's direction-specific. Low severity.

---

### P2 — Hardcoded 310 instead of PERIOD — line 265
```python
for j in range(max(310, i - LOOKBACK), i + 1):   # 310 should be PERIOD (300)
```
Works correctly (310 > 300, EMA valid), but hardcoded creates drift risk.

---

## Verified Correct (No Bug)
- gap_growth sign (lines 251-254): LONG `gap_now - gap_then` ✓, SHORT `gap_then - gap_now` ✓
- delta_last/delta_prev inequalities (lines 300-303): LONG `<=` and SHORT `>=` both correct
- abs(gap_now) check (line 223): correct for both directions
- Loop bounds (line 189): `-1` is intentional OOB protection
- Source names: `accel-300+` / `accel-300-` correctly assigned to LONG/SHORT
- Confidence floor/ceiling (lines 427-428): 60-min, 70-max correctly applied
- Per-direction kill-switches (lines 415-418): ACCEL_300_PLUS_ENABLED / ACCEL_300_MINUS_ENABLED checked
